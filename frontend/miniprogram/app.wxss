:root {
  --page-bg: #f4f6f8;
  --card-bg: #ffffff;
  --surface-soft: rgba(255, 255, 255, 0.78);
  --text-main: #16212f;
  --text-sub: #7f8b99;
  --text-faint: #a0acb8;
  --line: #e7ebf0;
  --brand: #07c160;
  --brand-soft: #e9f9ef;
  --brand-deep: #0f8e4a;
  --warning: #ff9f0a;
  --danger: #ee5a52;
  --shadow: 0 18rpx 40rpx rgba(22, 33, 47, 0.05);
  --radius-xl: 36rpx;
  --radius-lg: 24rpx;
  --radius-md: 18rpx;
  --radius-sm: 12rpx;
}

page {
  background: linear-gradient(180deg, #f8fbf8 0%, #f2f5f6 200rpx, #f4f6f8 100%);
  color: var(--text-main);
  font-size: 28rpx;
}

.page {
  min-height: 100vh;
  padding: 28rpx 24rpx 40rpx;
  box-sizing: border-box;
}

.card {
  background: var(--card-bg);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow);
  padding: 28rpx;
  box-sizing: border-box;
}

.section {
  margin-bottom: 28rpx;
}

.section-title {
  font-size: 32rpx;
  font-weight: 700;
}

.section-subtitle {
  color: var(--text-sub);
  font-size: 24rpx;
}

.section-kicker {
  display: inline-flex;
  align-items: center;
  gap: 10rpx;
  margin-bottom: 10rpx;
  color: var(--brand-deep);
  font-size: 22rpx;
  font-weight: 600;
  letter-spacing: 1rpx;
}

.name-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
  flex-wrap: wrap;
}

.user-tag-badge {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 8rpx 14rpx;
  border-radius: 999rpx;
  background: rgba(7, 193, 96, 0.12);
  color: #0f8e4a;
  font-size: 20rpx;
  font-weight: 700;
  line-height: 1;
}

.user-tag-badge.small {
  padding: 6rpx 12rpx;
  font-size: 18rpx;
}

.pill-tabs {
  display: flex;
  gap: 12rpx;
  margin-bottom: 18rpx;
  flex-wrap: wrap;
}

.pill-tab {
  padding: 12rpx 20rpx;
  border-radius: 999rpx;
  background: rgba(238, 243, 246, 0.95);
  color: var(--text-sub);
  font-size: 23rpx;
  transition: all 180ms ease;
}

.pill-tab.active {
  background: var(--brand-soft);
  color: var(--brand-deep);
  font-weight: 600;
}

.tag {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 8rpx 14rpx;
  border-radius: 999rpx;
  font-size: 22rpx;
  line-height: 1;
}

.tag.success {
  background: #e8f9ee;
  color: #05984a;
}

.tag.pending {
  background: #fff4e0;
  color: #d47a00;
}

.tag.danger {
  background: #ffeceb;
  color: #d9423c;
}

.tag.muted {
  background: #eef2f5;
  color: #7f8b99;
}

.empty {
  padding: 48rpx 24rpx;
  text-align: center;
  color: var(--text-sub);
}

.button-primary {
  background: linear-gradient(135deg, #07c160 0%, #06b258 100%);
  color: #ffffff;
  border-radius: 999rpx;
  font-size: 24rpx;
  border: none;
  box-shadow: 0 8rpx 16rpx rgba(7, 193, 96, 0.12);
  font-weight: 600;
}

.button-ghost {
  background: rgba(242, 245, 247, 0.96);
  color: var(--text-main);
  border-radius: 999rpx;
  border: none;
  font-size: 23rpx;
}

.button-primary::after,
.button-ghost::after,
.button-warn::after {
  border: none;
}

button.button-primary {
  background: linear-gradient(135deg, #07c160 0%, #06b258 100%);
  color: #ffffff;
}

button.button-ghost {
  background: rgba(242, 245, 247, 0.96);
  color: var(--text-main);
}

button.button-primary[disabled],
button.button-primary[loading] {
  background: #33c774;
  color: rgba(255, 255, 255, 0.98);
  box-shadow: none;
  opacity: 1;
}

.placeholder-avatar {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(160deg, #e5f7ec 0%, #dff4e7 100%);
  color: #07c160;
  font-weight: 700;
}

.small-avatar {
  font-size: 20rpx;
}

.button-warn {
  background: #fff2f1;
  color: var(--danger);
  border-radius: 999rpx;
  border: none;
  font-size: 23rpx;
}
