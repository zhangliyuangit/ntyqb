spring:
  datasource:
    url: jdbc:h2:mem:ntyqb-test;MODE=MYSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    show-sql: false

app:
  auth:
    mode: mock
    session-days: 14
  seed:
    enabled: true
  upload:
    root-dir: ./target/test-uploads
